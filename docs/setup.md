# Setup

1. Copy the `geor_append_field_map` folder into your QGIS plugins directory (usually `.../profiles/default/python/plugins`).
2. Restart QGIS or refresh the plugins list.
3. Enable **Append Features With Field Mapping** in *Plugins ► Manage and Install Plugins*.

The plugin does not add any GUI elements; it registers a processing algorithm.
