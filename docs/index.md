# Append Features With Field Mapping Plugin

This QGIS plugin provides a processing algorithm that appends features from a source layer to a target layer while allowing you to map fields between them.

The algorithm is available in the **Processing Toolbox** under *Append Features With Field Mapping*.
